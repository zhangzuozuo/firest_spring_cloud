spring:
  application:
    name: eurekaClient
  profiles:
    active: dev
  cloud:
      config:
        profile: dev
        name: eurekaclient
        label: master
        discovery:
          enabled: true
          service-id: config

